#!/usr/bin/env ruby

require_relative '../lib/pipeline'

def main
	if ARGV.length > 0 then
		pipeline = Pipeline.new(ARGV[0])
	else
		pipeline = Pipeline.new()
	end
	pipeline.parse()
	pipeline.run()
end

main
